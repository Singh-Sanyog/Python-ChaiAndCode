#  function with multiple parameters
#creating a function that take two nos. as parameters and return there sum 

def sum(a,b):
    return(a+b)

a=int(input("enter a no : "))
b=int(input("enter a no : "))
print(sum(a,b))